// as f32
// Rust does not automatically convert between number types.
// as f32 converts the integer width and height into floating-point numbers (f32), so you can multiply by a scaling factor s.

fn main() {
    println!("Hello, world!");
}